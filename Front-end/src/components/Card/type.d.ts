type EventCardProps = {
    type: 1 | 2,
};

type CampaignCardProps = {
    onClickDetail?: () => void,
}

type SupporterCardProps = {
    onClickDetail?: () => void,
}

type NewsCardProps = {
    onClickDetail?: () => void,
}